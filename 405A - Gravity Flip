#include <iostream>
#include <algorithm>
 
using namespace std;
int x, cu[100];
int main() {
    cin>>x;
    for (int i=0; i<x; i++) {
        cin>>cu[i];
    }
    sort(cu, cu+x);
    for (int i=0; i<x; i++) {
        cout<<cu[i]<<" ";
    }
 
    return 0;
}
